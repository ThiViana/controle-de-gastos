const CACHE_NAME = 'financas-v1';
self.addEventListener('install', (e) => {
  // Inicialização simples para conformidade PWA
});
self.addEventListener('fetch', (e) => {
  // Deixa as requisições passarem normalmente
});